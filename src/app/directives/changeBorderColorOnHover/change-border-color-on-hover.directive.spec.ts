import { ChangeBorderColorOnHoverDirective } from './change-border-color-on-hover.directive';

describe('ChangeBorderColorOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBorderColorOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
