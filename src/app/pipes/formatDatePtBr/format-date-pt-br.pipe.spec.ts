import { FormatDatePtBrPipe } from './format-date-pt-br.pipe';

describe('FormatDatePtBrPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDatePtBrPipe();
    expect(pipe).toBeTruthy();
  });
});
